CREATE OR REPLACE FUNCTION FORMAT_HORA_MINUTOS ( HORAS IN NUMBER) RETURN VARCHAR IS

RETORNO VARCHAR2(10):= '';
   
begin
	IF (HORAS is not null) THEN
		RETORNO := TO_CHAR(TRUNC(0.26),'FM99999990') || ':' || TO_CHAR(TRUNC(ROUND(MOD(0.26*3600,3600)/60)),'FM00');
	END IF;
     return RETORNO;
END;
/